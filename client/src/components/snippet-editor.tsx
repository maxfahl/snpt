import React, { FunctionComponent } from 'react'
import { Snippet } from "../models/snippet";

type SnippetEditorProps = {
	snippet: Snippet
}

const SnippetEditor: FunctionComponent<SnippetEditorProps> = ({ snippet }) => {
	return (
		<>

		</>
	);
};

export default SnippetEditor;
